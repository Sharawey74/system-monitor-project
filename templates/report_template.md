# System Observability Report
**Generated**: {{ generated_at }}

---

{% if native %}
## 🖥️ Windows Host (Native)
**Hostname**: {{ native.system.hostname }}
**OS**: {{ native.system.os }}
**Uptime**: {{ ((native.system.uptime_seconds | default(0) | int) / 3600) | round(1) }} hours

### Performance
- **CPU**: {{ native.cpu.usage_percent }}% ({{ native.cpu.model }})
- **Memory**: {{ native.memory.usage_percent }}% ({{ (native.memory.used_mb / 1024) | round(1) }} / {{ (native.memory.total_mb / 1024) | round(1) }} GB)
- **GPU Temp**: {{ native.gpu.devices[0].temperature_celsius if native.gpu.devices else 'N/A' }}°C

### Storage
| Device | Filesystem | Usage | Size |
|--------|------------|-------|------|
{% for disk in native.disk -%}
| {{ disk.device }} | {{ disk.filesystem }} | {{ disk.used_percent | round(1) }}% | {{ (disk.total_gb) | round(0) }} GB |
{% endfor %}
{% endif %}

---

{% if legacy %}
## 🐧 WSL2 Guest (Virtual)
**Hostname**: {{ legacy.system.hostname }}
**OS**: {{ legacy.system.os }}
**Kernel**: {{ legacy.system.kernel }}

### Performance
- **vCPU**: {{ legacy.cpu.usage_percent }}% (Load: {{ legacy.cpu.load_1 }} | Temp: {{ legacy.temperature.cpu_celsius | default('N/A') }}°C)
- **Memory**: {{ legacy.memory.usage_percent }}% ({{ (legacy.memory.used_mb / 1024) | round(1) }} / {{ (legacy.memory.total_mb / 1024) | round(1) }} GB)
{% if legacy.gpu and legacy.gpu.devices %}
- **GPU**: {{ legacy.gpu.devices[0].model | default('Unknown') }} ({{ legacy.gpu.devices[0].temperature_celsius | default('N/A') }}°C | {{ legacy.gpu.devices[0].utilization_percent | default(0) }}%)
{% endif %}

### Storage
| Mount | Usage | Used |
|-------|-------|------|
{% for disk in legacy.disk -%}
{% if disk.device == '/' or disk.device.startswith('/mnt') -%}
| {{ disk.device }} | {{ disk.used_percent | round(1) }}% | {{ disk.used_gb | round(1) }} GB |
{% endif -%}
{% endfor %}
{% endif %}

---

{% if alerts %}
## ⚠️ System Alerts
{% for alert in alerts %}
- **[{{ alert.level|upper }}]**: {{ alert.message }}
{% endfor %}
{% endif %}

*Generated by System Monitor v5.0*
